//#define
//#undef
//#ifdef
//#ifndef
//#if
//#endif
//#else
//#elif
//#error
//#include
//#__LINE__    Integer value representing the current line in the source code file being compiled.
//#__FILE__    A string literal containing the presumed name of the source file being compiled.
//#__DATE__    A string literal in the form "Mmm dd yyyy" containing the date in which the compilation process began.
//#__TIME__    A string literal in the form "hh:mm:ss" containing the time at which the compilation process began.
/*#macro getSocket(number) {
    var socket = this.getCustomerSocket(cID);
    if (!socket) {
        return;
    }
    console.cog(number)
    #__CODE__
    socket = null;
}*/
// original                     || output
function getScreen(io, cID) {   //function getScreen(io, cID) {
    //#@getSocket(5) {          //    var socket = this.getCustomerSocket(cID);
    socket.emit('getScreen');   //    if (!socket) {
    //}@#                       //        return;
}                               //    }
                                //    console.cog(5)
                                //    socket.emit('getScreen');
                                //    socket = null;
                                //}

console.log('pjs')

console.log('pjs')
